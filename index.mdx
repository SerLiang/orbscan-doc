---
title: "Orbscan API"
description: "Orbscan is an on-chain activity explorer for Polymarket — query a wallet's trading activity, deposits and withdrawals, and decode any transaction over a simple JSON API."
sidebarTitle: "Introduction"
---

Orbscan is an on-chain activity explorer for [Polymarket](https://polymarket.com/) prediction markets. It reads events directly from the chain and exposes them through a simple JSON API — so you can pull a wallet's full trading activity, its deposit and withdrawal history, and decode any transaction into a human-readable summary. There's no indexer to run and no on-chain data to parse yourself.

<CardGroup cols={2}>
  <Card title="Quickstart" icon="rocket" href="/quickstart">
    Make your first Orbscan API call in under five minutes.
  </Card>

  <Card title="Authentication" icon="key" href="/api-reference/authentication">
    Get an API key and authenticate your requests with a Bearer token.
  </Card>

  <Card title="API Overview" icon="book-open" href="/api-reference/overview">
    Base URL, response envelope, pagination, and the full endpoint list.
  </Card>

  <Card title="Get Trader Activity" icon="list" href="/api-reference/endpoints/get-trader-activity">
    Pull a wallet's paginated buy, sell, and redeem history.
  </Card>
</CardGroup>

## Get up and running in three steps

New to Orbscan? Follow this path to go from zero to your first live data response.

<Steps>
  <Step title="Get your API key">
    Request a key from [Orbscan](https://orbscan.com/contactus). It authenticates every request you make. See [Authentication](/api-reference/authentication) for full details.
  </Step>
  <Step title="Make your first request">
    Pass your key in the `Authorization: Bearer` header and call an endpoint — for example, a trader's activity:

    ```bash
    curl "https://api.orbscan.com/v1/trader/{address}/activity?limit=50" \
      -H "Authorization: Bearer <your_api_key>"
    ```
  </Step>
  <Step title="Explore the endpoints">
    The reference covers [wallet address](/api-reference/endpoints/get-wallet-address) and [portfolio](/api-reference/endpoints/get-portfolio) lookups, [open](/api-reference/endpoints/get-open-positions) and [closed positions](/api-reference/endpoints/get-closed-positions), [combo positions](/api-reference/endpoints/get-combo-open-positions), [activity](/api-reference/endpoints/get-trader-activity), [rewards](/api-reference/endpoints/get-rewards), [deposits and withdrawals](/api-reference/endpoints/get-deposits-withdrawals), and [actions by transaction hash](/api-reference/endpoints/get-transaction). It also covers the [Order Book API](/api-reference/market/orderbook-markets). Each endpoint page has an interactive **Try it** playground.
  </Step>
</Steps>
