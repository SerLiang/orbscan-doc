---
title: "Authentication"
description: "Request an API key to use the Orbscan API."
---

Every request requires an API key. Request one through [Orbscan](https://orbscan.com) before calling an endpoint.

Pass your key using the `Authorization` header. Keep it private and do not commit it to source control.
