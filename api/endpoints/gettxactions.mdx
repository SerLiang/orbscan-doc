---
title: "Get actions by transaction hash"
description: "Decode a transaction into a human-readable summary and its actions."
---

## GET `/api/v1/gettxactions`

Decodes a transaction hash into a human-readable summary and the individual actions it contains.

<Note>Every request requires an API key. See [Authentication](/).</Note>
